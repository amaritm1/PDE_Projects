Hello Amarit, your finite elements program works quite well.
Here are a couple of minor comments.

1. In the makefile the dependency lines (the lines generated by cc -MM)
are missing.  As a result, the makefile is not working properly.

To see the issue, type "make" to compile your program.
Then type "make" once again.  You will get:
    make: 'fem-demo' is up to date.
which is what we expect.  Now, edit your poisson.h, making some
benign changes in it, and save.  Type "make" again.
This time we also get
    make: 'fem-demo' is up to date.
which is /not/ what we expect -- poisson.h has been changed so the
program needs to be recompiled.  This is not happening because of
the missing dependency lines in the makefile.

2. In poisson.c, compute_element_stiffness()
The fabs() is redundant since its argument is always positive.

Same goes for the fabs() in the last two lines of elem_errors().

Grade for this assignment: "A"